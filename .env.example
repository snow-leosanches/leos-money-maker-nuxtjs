# Copy to .env and set values as needed.
# Nuxt loads .env automatically (see https://nuxt.com/docs/guide/going-further/runtime-config#environment-variables).

# Snowplow collector URL (optional). When set, enables Snowplow analytics and page view tracking.
# NUXT_PUBLIC_SNOWPLOW_COLLECTOR_URL=https://your-collector.example.com
